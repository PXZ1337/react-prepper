import { Fragment } from "react"
import BackButton from "../../components/UI/BackButton"

const StockAddPage = () => {
    return <Fragment>
        <BackButton />
        <h1>This is the prepper stock add page</h1>
    </Fragment>
}

export default StockAddPage